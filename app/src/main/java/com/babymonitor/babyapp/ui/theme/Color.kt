package com.babymonitor.babyapp.ui.theme

import androidx.compose.ui.graphics.Color

// --- Baby Monitor Theme Colors (Soft and Calming) ---

// Primary: Soft, inviting accent color for buttons and highlights (Bright Pink/Rose)
val PrimarySoft = Color(0xFFFF70A6)
val OnPrimary = Color(0xFFFFFFFF)

// Secondary: Calming color for surfaces, chip backgrounds, and secondary actions (Pale Sky Blue)
val SecondaryCalm = Color(0xFFD0EFFF)
val OnSecondary = Color(0xFF001F2A)

// Tertiary: Subtle accent for data visualization or minor elements (Soft Mint Green)
val TertiarySubtle = Color(0xFFA5E6A5)
val OnTertiary = Color(0xFF00210A)

// Background & Surface: Gentle, bright colors for high contrast
val BackgroundLight = Color(0xFFFAFAFA) // Almost white background
val SurfaceLight = Color(0xFFFFFFFF) // Pure White for cards/containers
val OnBackground = Color(0xFF1E1E1E) // Dark text for readability
val OnSurface = Color(0xFF1E1E1E)

// Error Color (Standard)
val ErrorRed = Color(0xFFBA1A1A)
val OnError = Color(0xFFFFFFFF)

// Old default colors removed for clarity, focusing only on the custom theme.
